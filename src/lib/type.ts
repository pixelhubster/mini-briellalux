
type IProduct = {
    _id: string,
    name: string,
    description: string,
    price: number,
    images: string[],
    shipping: string,
    quantity: number,
    category: string,
}

export type {IProduct};